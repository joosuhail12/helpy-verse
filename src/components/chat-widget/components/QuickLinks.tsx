
import React from 'react';

/**
 * Quick links section for the chat widget - removed as per new design
 */
const QuickLinks = () => {
  // This component is now empty as per the new design screenshot
  return null;
};

export default QuickLinks;
