
import * as React from 'react';
import WorkflowsPage from './WorkflowsPage';

/**
 * Container for the Workflows page
 */
const Workflows: React.FC = () => {
  return <WorkflowsPage />;
};

export default Workflows;
