
// Export components directly rather than using dynamic imports
export { ChatWidget } from './ChatWidget';
export type { 
  ThemeConfig, 
  ChatWidgetSettings, 
  View,
  WidgetOptions
} from './types';
